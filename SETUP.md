# 🚀 Инструкция по запуску Admin Panel Chat Bot

## ✅ Что уже сделано

### Backend (Laravel)
- ✅ Установлен Laravel Sanctum
- ✅ Создана архитектура Service-Repository-Controller
- ✅ Реализована авторизация (регистрация, вход, выход)
- ✅ Созданы API роуты (`/api/auth/*`)
- ✅ Настроена конфигурация Sanctum

### Frontend (Vue 3 + TypeScript)
- ✅ Установлены Pinia и Vue Router
- ✅ Создана структура папок (components, layouts, views, stores, services)
- ✅ Реализованы страницы Login и Register
- ✅ Создана главная страница Dashboard
- ✅ Настроена защита маршрутов
- ✅ Создана модульная структура SCSS стилей

## 📋 Шаги для запуска

### 1. Выполнить миграции базы данных

Откройте терминал в корне проекта и выполните:

```bash
php artisan migrate
```

Это создаст таблицы:
- `users` - пользователи
- `personal_access_tokens` - токены Sanctum для авторизации
- `cache`, `jobs` - служебные таблицы Laravel

### 2. Скомпилировать Frontend

```bash
npm run build
```

Или для режима разработки:

```bash
npm run dev
```

### 3. Запустить Laravel сервер

Если используете OSPanel, убедитесь что:
- Apache запущен
- MySQL запущен
- Виртуальный хост настроен на папку `public/`

Или запустите встроенный сервер:

```bash
php artisan serve
```

## 🎯 Структура проекта

### Backend
```
app/
├── Http/
│   ├── Controllers/
│   │   └── AuthController.php       # Контроллер авторизации
│   ├── Requests/
│   │   ├── LoginRequest.php         # Валидация входа
│   │   └── RegisterRequest.php      # Валидация регистрации
│   └── Resources/
│       └── UserResource.php         # Сериализация пользователя
├── Services/
│   └── AuthService.php              # Бизнес-логика авторизации
├── Repositories/
│   └── UserRepository.php           # Работа с БД
└── Models/
    └── User.php                     # Модель пользователя (с HasApiTokens)
```

### Frontend
```
resources/
├── js/
│   ├── components/                  # Vue компоненты
│   ├── layouts/
│   │   └── MainLayout.vue           # Основной layout с навигацией
│   ├── views/
│   │   ├── Login.vue                # Страница входа
│   │   ├── Register.vue             # Страница регистрации
│   │   └── Dashboard.vue            # Главная страница
│   ├── stores/
│   │   └── authStore.ts             # Pinia store для авторизации
│   ├── services/
│   │   ├── api.ts                   # Axios конфигурация
│   │   └── authService.ts           # API методы авторизации
│   ├── composables/
│   │   └── useAuth.ts               # Composable для авторизации
│   ├── router/
│   │   └── index.ts                 # Vue Router с защитой маршрутов
│   ├── types/
│   │   └── index.ts                 # TypeScript типы
│   ├── App.vue                      # Главный компонент
│   ├── app.ts                       # Точка входа
│   └── bootstrap.ts                 # Axios + CSRF настройка
└── css/
    ├── abstracts/                   # Variables, mixins
    ├── base/                        # Reset, utilities
    ├── components/                  # Buttons, forms
    ├── layouts/                     # Layout styles
    ├── pages/                       # Page-specific styles
    └── app.scss                     # Main SCSS файл
```

## 🔌 API Endpoints

### Публичные
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Защищенные (требуют Bearer token)
- `GET /api/auth/me` - Получить текущего пользователя
- `POST /api/auth/logout` - Выход
- `POST /api/auth/logout-all` - Выход со всех устройств

## 🎨 Архитектура

### Backend: Service-Repository-Controller Pattern
1. **Controller** - принимает HTTP запросы, валидирует через Requests
2. **Service** - содержит бизнес-логику
3. **Repository** - работа с базой данных
4. **Resource** - сериализация ответов API

### Frontend: Composition API + Pinia
1. **Views** - страницы приложения
2. **Layouts** - обертки для страниц
3. **Stores (Pinia)** - глобальное состояние
4. **Composables** - переиспользуемая логика
5. **Services** - API запросы
6. **Router** - навигация с guards

## 🔐 Авторизация

Используется **Laravel Sanctum** с Bearer Token:
1. Пользователь логинится → получает token
2. Token сохраняется в localStorage
3. При каждом запросе token добавляется в заголовок `Authorization: Bearer {token}`
4. Backend проверяет token через middleware `auth:sanctum`

## 🛠️ Команды для разработки

```bash
# Frontend разработка (hot reload)
npm run dev

# Сборка для продакшена
npm run build

# TypeScript проверка типов
npm run type-check

# Laravel миграции
php artisan migrate

# Создать нового пользователя (опционально)
php artisan tinker
>>> User::create(['name' => 'Admin', 'email' => 'admin@example.com', 'password' => bcrypt('password')])
```

## 📝 Тестирование

1. Откройте браузер: `http://localhost` (или ваш виртуальный хост)
2. Вы будете перенаправлены на `/login`
3. Нажмите "Зарегистрируйтесь" и создайте аккаунт
4. После регистрации вы попадете на Dashboard
5. Попробуйте выйти и войти снова

## 🎉 Готово!

Проект полностью настроен и готов к работе. Вы можете:
- Расширять функционал, добавляя новые контроллеры и страницы
- Добавлять новые роуты в `routes/api.php`
- Создавать новые Vue компоненты
- Добавлять стили в соответствующие SCSS файлы

---

**База данных:** MySQL (AdminPanelChatBot)  
**Конфигурация:** `.env` (строки 25-32)

